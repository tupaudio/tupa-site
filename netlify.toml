[build]
  command = "npm run build"
 
[context.production.environment]
  # Isso desabilita o scanner apenas para o ambiente de produção
  NETLIFY_SKIP_SECRET_SCAN = "true"
 
[[plugins]]
  package = "@netlify/plugin-nextjs"